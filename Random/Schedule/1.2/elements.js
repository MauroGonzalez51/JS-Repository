import { selectRange } from "./selectRange.js";
import { getAllStorageData } from "./localStorage.js";

const createElements = (arrRange) => {
    console.log(getAllStorageData());
};

export { createElements };
